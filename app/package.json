{
  "name": "hello-courier",
  "version": "1.0.0",
  "private": true,
  "description": "📮 zero-dependency demo app for learn-cicd-school",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "prepare:slow": "node prepare.js",
    "test": "node --test",
    "test:ci": "node --test --test-reporter=spec --test-reporter-destination=stdout --test-reporter=junit --test-reporter-destination=test-results.xml"
  }
}
